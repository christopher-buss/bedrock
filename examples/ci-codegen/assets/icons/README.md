# Icons

`bedrock.config.ts` points at `vip-pass.png` here. Supply your own 512x512 image
before deploying — the file is not committed because there is nothing generic to
commit.

Bedrock hashes the file contents and records the hash in state, so an unchanged
icon is never re-uploaded.
