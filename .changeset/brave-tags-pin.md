---
---

Pin the baked deploy composite's commit-back ref to its exact release tag and document pre-1.0 exact-tag pinning. Release plumbing for the private `@bedrock-rbx/actions` package; no npm release.
