---
"@bedrock-rbx/ocale": patch
---

Let a caller observe when one request is held for SDK-managed admission (operation queue, reported budget, or retry delay) through a per-request `onAdmissionWait` observer.
