---
---

Inline `@actions/core` into the commit-back action bundle so the baked dist runs on the node24 runner without `node_modules`. `@bedrock-rbx/actions` is private and ships via `actions-v*` tags, so no package release is needed.
